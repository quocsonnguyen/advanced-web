import React from 'react';
import { Layout } from '../../common'

const Manage = (props) => {
    return (
        <Layout>
            <button>Tao tai khoan</button>
            <button>Cap quyen</button>
        </Layout>
    );
}

export default Manage;