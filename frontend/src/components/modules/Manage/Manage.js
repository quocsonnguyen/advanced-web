import React from 'react';
import { Layout } from '../../common'
import { RegisterPage } from '../../common';
//import Authorize from '../../common/AuthorizePage/authorize';
const Manage = (props) => {
    return (
        <Layout>
            <RegisterPage/>
        </Layout>
    );
}

export default Manage;